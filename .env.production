# Production Environment Variables for Vercel
# After deployment, set these in Vercel Dashboard: Settings → Environment Variables

# n8n Webhook URL for email classification (your actual n8n production URL)
VITE_N8N_WEBHOOK_URL=https://jldev.app.n8n.cloud/webhook/test-email-classification

# Email API URL - This should point to your deployed backend API
# Option 1: Deploy backend to Vercel as separate project
# Option 2: Use n8n webhook directly to fetch emails
# Option 3: Use a cloud database service
VITE_EMAIL_API_URL=https://jldev.app.n8n.cloud/webhook/get-emails

# Use mock processing - Set to false for production
VITE_USE_MOCK=false
