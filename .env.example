# Email Processing Configuration

# n8n Webhook URL for email classification - Set this to your n8n workflow webhook endpoint
# Example: https://your-n8n-instance.com/webhook/test-email-classification
VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook/test-email-classification

# Email API URL - Endpoint to fetch emails from your backend/n8n
# This should return a list of emails in the Email[] format
VITE_EMAIL_API_URL=http://localhost:5678/webhook/get-emails

# Use mock processing (set to 'true' for development without n8n, 'false' for production)
VITE_USE_MOCK=false
